## Use Case 001 - Play Game

- **Primary Actor:** Player
- **Description:** The player starts the game.
- **Preconditions:** The player has launched the game via the binary.
- **Main Success Scenario:**
  1. The `initialScene` loads via `Alert` dialog with `playGame` `Button` and `exit` `Button`.
  2. The player clicks the `playGame` `Button`.
  3. The `GameController` is `initialize`d.
    1. The `shotCounter` is initialized to `0`.
    2. The `totalTime` is initialized to `10` seconds.
    3. The `targetCounter` is initialized to `9`.
  6. The `Canvas` loads, displaying a `Cannon`, a `Blocker`, `totalTime`, and `9` `Target`s.
  3. The `GameController.timer()` starts counting down.
- **Alternative Flows:**
  - Player clicks the `exit` `Button`, the game closes gracefully.